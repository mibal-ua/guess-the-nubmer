### Important run info:

#### You can use `start.sh` script with arguments (you can see list below):

1. Open the terminal in folder with scripts.
2. Enter command `./start.sh ${your-arguments}`.
3. List of arguments:
   - User Interface: `GUI`, `CONSOLE`;

#### Example of use: `./start.sh GUI`

- User interface became `GUI`.

author: @mibal_ua | #StandWithUkraine